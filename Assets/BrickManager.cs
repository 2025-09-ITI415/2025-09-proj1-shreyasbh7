using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BrickManager : MonoBehaviour
{
    //public GameObject brickPrefab; // assign in Inspector
    //public int rows = 5;
    //public int cols = 10;
    //public float brickWidth = 0.6f;
    //public float brickHeight = 0.25f;
    //public float padding = 0.1f;
    //public Vector2 startOffset = new Vector2(0f, 0f);

    



    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //GenerateBricks();
    }


    //void GenerateBricks()
    //{
        //for (int row = 0; row < rows; row++)
        //{
            //for (int col = 0; col < cols; col++)
            //{
                //Vector3 position = new Vector3(
                    //startOffset.x + col * (brickWidth + padding),
                    //startOffset.y + row * (brickHeight + padding),
                    //0f
                //);
                //Instantiate(brickPrefab, position, Quaternion.identity, this.transform);
            //}
        //}
    //}
    // Update is called once per frame
    void Update()
    {

    }
}
